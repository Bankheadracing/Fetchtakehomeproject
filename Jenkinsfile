pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Starting the build process...'
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests on application...'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying the application...'
            }
        }
    }
}
